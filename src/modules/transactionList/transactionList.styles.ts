import { StyleSheet } from "react-native";
import create = StyleSheet.create;
import { INDENT } from "../../constants/styles";

export const transactionListStyles = create({
    wrapper: {
        marginBottom: INDENT.SMALL,
    },
});
