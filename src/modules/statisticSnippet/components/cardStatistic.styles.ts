import { StyleSheet } from "react-native";
import create = StyleSheet.create;
import { COLORS } from "../../../constants/styles";

export const cardStatisticStyles = create({
    cardGraph: {
        height: 50,
        width: 60,
        marginLeft: "auto",
    },
});
